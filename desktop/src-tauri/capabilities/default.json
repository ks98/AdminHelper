{
  "identifier": "default",
  "description": "Default capabilities for AdminHelper desktop client",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "binaries/frpc", "sidecar": true, "args": true }
      ]
    }
  ]
}
